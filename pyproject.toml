[project]
name = "verilog-a-doom"
version = "0.1.0"
description = "extending my course project to try to run doom"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "pytest>=9.1.1",
]
